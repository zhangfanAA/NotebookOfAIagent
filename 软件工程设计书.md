# 智能学习助手（SmartRead）软件工程设计书

---

## 目录

1. [引言](#1-引言)
2. [系统概述](#2-系统概述)
3. [需求分析](#3-需求分析)
4. [系统架构设计](#4-系统架构设计)
5. [数据库设计](#5-数据库设计)
6. [接口设计](#6-接口设计)
7. [模块详细设计](#7-模块详细设计)
8. [安全设计](#8-安全设计)
9. [部署设计](#9-部署设计)
10. [测试设计](#10-测试设计)

---

## 1. 引言

### 1.1 项目背景

随着高等教育信息化的深入推进，学生面临的学习资料日益增多，传统的"阅读-记忆"学习模式效率低下。人工智能技术的发展为个性化、交互式学习提供了新的可能。本项目旨在利用 RAG（检索增强生成）技术，构建一款能够理解课件内容、支持自然语言问答的智能学习助手，帮助学生更高效地掌握知识。

### 1.2 项目目的

本系统的目标是：

1. 实现基于课件/教材的智能问答，让学生通过自然语言与学习资料交互
2. 提供思维导图生成、测验、闪卡等辅助学习工具，丰富学习方式
3. 构建多 Agent 协同架构，支持复杂学习任务的自动化编排
4. 提供记忆模式和知识诊断，实现个性化的学习路径推荐

### 1.3 项目范围

本设计书覆盖 SmartRead 系统的完整软件设计，包括：

- 后端服务（FastAPI REST API + Supervisor Agent）
- Web 前端（Next.js）及桌面端前端（Electron）
- OCR 服务（独立微服务）
- 数据库设计（MySQL + ChromaDB）
- 部署方案（Docker Compose）

### 1.4 术语定义

| 术语 | 定义 |
|------|------|
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| LLM | Large Language Model，大语言模型 |
| MCP | Model Context Protocol，模型上下文协议 |
| SSE | Server-Sent Events，服务器推送事件 |
| LangGraph | 基于 LangChain 的有状态图执行框架 |
| ChromaDB | 开源向量数据库 |
| OCR | Optical Character Recognition，光学字符识别 |
| ONNX | Open Neural Network Exchange，开放神经网络交换格式 |
| ReAct | Reasoning and Acting，推理与行动 Agent 范式 |
| JWT | JSON Web Token，JSON 网络令牌 |

### 1.5 参考文献

- FastAPI 官方文档：https://fastapi.tiangolo.com/
- LangGraph 官方文档：https://langchain-ai.github.io/langgraph/
- ChromaDB 官方文档：https://docs.trychroma.com/
- MCP 协议规范：https://spec.modelcontextprotocol.io/
- Next.js 官方文档：https://nextjs.org/docs

---

## 2. 系统概述

### 2.1 系统定位

SmartRead 是一款面向高校学生的 AI 智能学习助手，核心能力是基于 RAG 技术的课件智能问答。系统采用前后端分离架构，后端基于 Python FastAPI，前端基于 Next.js/React，通过 REST API 和 SSE 流式通信。

### 2.2 用户角色

| 角色 | 权限 | 说明 |
|------|------|------|
| 普通用户 (role=1) | 基础功能 | 上传文档、智能问答、思维导图、测验等 |
| 管理员 (role=2) | 全部功能 | 用户管理、余额管理、全局配置、OCR 配置等 |

### 2.3 技术栈总览

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 前端 | Next.js 15 + React 19 + TypeScript | App Router，Tailwind CSS + shadcn/ui |
| 桌面端 | Electron + Vite + React | 独立打包 |
| 后端 | Python FastAPI | REST API，异步支持，SSE 流式输出 |
| OCR 服务 | Python FastAPI（独立项目） | PaddleOCR 云端/本地识别 |
| 数据库 | MySQL 8.0 | PyMySQL + DBUtils 连接池 |
| 向量数据库 | ChromaDB | 本地持久化存储 |
| 嵌入模型 | BGE-small-zh-v1.5 (ONNX Runtime) | 文本向量化，CPU 推理 |
| LLM | Ollama (本地) / DeepSeek (云端) / 余额模型 | 三模式架构 |
| Agent 框架 | LangGraph + LangChain | 状态机驱动的 RAG 工作流 |
| MCP 框架 | mcp (FastMCP) | MCP Server 封装 |
| 部署 | Docker Compose | MySQL + FastAPI + Next.js 三容器编排 |

### 2.4 系统上下文图

```
                    ┌──────────────┐
                    │   学生用户    │
                    │  (浏览器/桌面)│
                    └──────┬───────┘
                           │ HTTP/SSE
                    ┌──────┴───────┐
                    │  SmartRead   │
                    │   前端应用    │
                    └──────┬───────┘
                           │ REST API
                    ┌──────┴───────┐
                    │  SmartRead   │         ┌──────────────┐
                    │   后端服务    │────────→│  OCR 服务器   │
                    └──────┬───────┘  HTTP   └──────────────┘
                           │
                    ┌──────┴───────┐
                    │  管理员用户   │
                    └──────────────┘
```

---

## 3. 需求分析

### 3.1 功能需求

#### 3.1.1 用户管理模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-01 | 用户注册（需管理员开启注册开关） | 高 |
| FR-02 | 用户登录（JWT Token 认证） | 高 |
| FR-03 | 管理员封禁/解封用户 | 高 |
| FR-04 | 用户余额管理（管理员调整、自动扣费） | 中 |
| FR-05 | 用户使用记录查看 | 中 |

#### 3.1.2 智能问答模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-10 | 基于上传文档的 RAG 智能问答 | 高 |
| FR-11 | SSE 流式输出，逐 token 显示 | 高 |
| FR-12 | 回答附带引用来源（文档名、页码、置信度） | 高 |
| FR-13 | 记忆模式：检索历史问答向量注入上下文 | 中 |
| FR-14 | 多轮对话上下文理解 | 中 |
| FR-15 | 查询自动改写（指代词消解） | 低 |

#### 3.1.3 文档管理模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-20 | PDF 文档上传（最大 100MB） | 高 |
| FR-21 | 文档自动解析（PyMuPDF + OCR 兜底） | 高 |
| FR-22 | 文档向量化入库（BGE 嵌入 + ChromaDB） | 高 |
| FR-23 | 文档列表查看、删除 | 高 |
| FR-24 | 扫描型 PDF OCR 识别（本地/云端） | 中 |

#### 3.1.4 学习工具模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-30 | 思维导图/重点笔记自动生成（Mermaid 格式） | 高 |
| FR-31 | 测验生成（选择题/填空题/简答题） | 高 |
| FR-32 | 测验自动判分 | 中 |
| FR-33 | 闪卡自动生成（正面/背面） | 中 |
| FR-34 | 文档对比分析 | 低 |
| FR-35 | 知识诊断（连续提问同知识点触发建议） | 低 |

#### 3.1.5 Agent 模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-40 | Supervisor Agent 多工具调度 | 高 |
| FR-41 | Agent 思考链可视化（工具调用过程展示） | 高 |
| FR-42 | Agent SSE 流式事件（tool_call/tool_result/token/done） | 高 |
| FR-43 | Agent 工具 user_id 自动注入（防越权） | 高 |

#### 3.1.6 辅助功能模块

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| FR-50 | 阅读进度记录 | 低 |
| FR-51 | 书签管理 | 低 |
| FR-52 | 会话笔记 | 低 |
| FR-53 | 消息收藏 | 低 |
| FR-54 | 标签系统 | 低 |
| FR-55 | 会话导出（Markdown/HTML） | 低 |
| FR-56 | 站内信系统 | 低 |
| FR-57 | 文件下载区 | 低 |

### 3.2 非功能需求

| 编号 | 需求描述 | 指标 |
|------|----------|------|
| NFR-01 | 响应时间 | 普通 API < 500ms，RAG 问答 < 30s |
| NFR-02 | 并发支持 | 支持 30 请求/分钟/用户 |
| NFR-03 | 数据安全 | JWT 认证、bcrypt 密码哈希、参数化查询 |
| NFR-04 | 用户隔离 | 所有数据按 user_id 隔离 |
| NFR-05 | 可用性 | Docker Compose 一键部署 |
| NFR-06 | 可扩展性 | MCP 协议支持外部工具接入 |
| NFR-07 | 文件限制 | 上传文件最大 100MB |
| NFR-08 | 日志记录 | 分级日志（DEBUG/INFO/ERROR），文件轮转 |

---

## 4. 系统架构设计

### 4.1 整体架构

系统采用分层架构，共分为五层：

```
┌─────────────────────────────────────────────────────────┐
│                    表示层 (Presentation)                   │
│  Next.js Web 前端 / Electron 桌面端                       │
└────────┬────────────────────────────┬────────────────────┘
         │ HTTP/REST + SSE            │ HTTP/REST + SSE
┌────────┴────────────────────────────┴────────────────────┐
│                    接口层 (API Gateway)                    │
│  FastAPI 路由 (/api/*) + Agent 路由 (/api/agent/*)        │
│  JWT 认证 + 速率限制 + 权限校验                             │
└────────┬────────────────────────────┬────────────────────┘
         │                            │
┌────────┴──────────────┐  ┌─────────┴───────────────────┐
│   业务逻辑层 (Service) │  │   Agent 编排层 (Orchestration)│
│  RAGService 门面       │  │  SupervisorGraph (LangGraph) │
│  QueryEngine           │  │  ToolNode + ChatOpenAI       │
│  DocumentManager       │  │  MCP Server (HTTP/SSE)       │
│  SessionManager        │  │                              │
└────────┬──────────────┘  └─────────┬───────────────────┘
         │                            │
┌────────┴────────────────────────────┴────────────────────┐
│                    数据访问层 (Data Access)                 │
│  MySQL Repository (user/session/document/...)              │
│  ChromaDB 向量存储                                         │
│  文件系统 (PDF 存储)                                        │
└─────────────────────────────────────────────────────────┘
```

### 4.2 后端模块结构

```
Backend/src/
├── api/                    # 接口层
│   ├── auth.py            # JWT 认证、密码校验
│   └── routes.py          # 全部 API 路由定义
├── config.py              # 全局配置管理
├── logger.py              # 日志模块
├── data/                   # 数据处理层
│   ├── pdf_parser.py      # PDF 解析（PyMuPDF + OCR）
│   ├── chunker.py         # 文本切片
│   └── vector_store.py    # ChromaDB 向量存储
├── database/               # 数据访问层
│   ├── db_manager.py      # MySQL 连接池、建表 DDL
│   ├── user_repo.py       # 用户数据仓库
│   ├── session_repo.py    # 会话/消息数据仓库
│   ├── document_repo.py   # 文档数据仓库
│   ├── mindmap_repo.py    # 思维导图数据仓库
│   ├── quiz_repo.py       # 测验数据仓库
│   ├── flashcard_repo.py  # 闪卡数据仓库
│   ├── global_config_repo.py # 全局配置数据仓库
│   ├── message_repo.py    # 站内信数据仓库
│   └── download_repo.py   # 下载文件数据仓库
├── rag/                    # RAG 核心层
│   ├── rag_service.py     # RAG 服务门面（统一入口）
│   ├── query_engine.py    # 查询引擎（检索+生成+记忆）
│   ├── llm_client.py      # LLM 客户端（三模式）
│   ├── chat_history_store.py # 聊天历史向量存储
│   ├── session_manager.py # 会话管理
│   ├── document_manager.py # 文档管理
│   ├── state.py           # LangGraph 状态定义
│   ├── graph.py           # LangGraph 状态机组装
│   ├── retrieve.py        # 检索节点
│   ├── grade.py           # 相关性评估节点
│   ├── generate.py        # 答案生成节点
│   ├── rewrite.py         # 查询重写节点
│   ├── diagnosis.py       # 知识诊断
│   ├── flashcard_generator.py # 闪卡生成器
│   └── quiz_generator.py  # 测验生成器
├── mcp_servers/            # MCP Server 层
│   ├── rag_server.py      # RAG MCP Server
│   ├── learning_tools_server.py # 学习工具 MCP Server
│   └── ocr_server.py      # OCR MCP Server
├── supervisor/             # Supervisor Agent 层
│   ├── prompts.py         # Agent 系统提示词
│   └── graph.py           # SupervisorGraph（ReAct Agent）
└── utils/                  # 工具层
    ├── security.py        # 安全工具（输入校验、注入检测）
    ├── startup_check.py   # 启动检查
    └── export.py          # 会话导出
```

### 4.3 RAG 状态机设计

RAG 查询采用 LangGraph 图结构实现循环自省机制：

```
START
  │
  ▼
┌──────────┐     ┌──────────┐
│ Retrieve │────→│  Grade   │
│  (检索)   │     │  (评估)   │
└──────────┘     └────┬─────┘
                      │
              ┌───────┴───────┐
              │               │
           "yes"           "no"
              │               │
              ▼               ▼
        ┌──────────┐    ┌──────────┐
        │ Generate │    │  Rewrite │
        │  (生成)   │    │  (重写)   │
        └────┬─────┘    └────┬─────┘
             │               │
             ▼               │
           END          ┌────┘
                        │ (循环，最多 3 次)
                        ▼
                   回到 Retrieve
```

**状态定义（AgentState）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| question | str | 用户输入的查询（含历史上下文） |
| rewritten_query | str \| None | 重写后的查询 |
| documents | list \| None | 检索到的文档片段 |
| relevance | str \| None | 评估结果 ("yes"/"no") |
| answer | str \| None | 生成的回答 |
| sources | list \| None | 引用来源 |
| reasoning | str \| None | 推理过程 |
| confidence | float \| None | 置信度 |
| loop_count | int | 当前循环次数 |
| max_loops | int | 最大循环次数 |
| session_id | str | 会话 ID |
| user_id | int | 用户 ID |

### 4.4 Supervisor Agent 设计

Supervisor Agent 基于 LangGraph ReAct 范式，通过 tool-calling 机制调度 MCP 工具：

```
用户问题
    │
    ▼
┌──────────────┐
│  Agent 节点   │ ← System Prompt (SUPERVISOR_PROMPT)
│  (ChatOpenAI) │
└──────┬───────┘
       │
  ┌────┴────┐
  │         │
tool_calls  无 tool_calls
  │         │
  ▼         ▼
┌────────┐  END
│ToolNode│  (输出最终答案)
│(6个工具)│
└────┬───┘
     │ 工具结果
     ▼
 回到 Agent 节点
```

**可用工具（6 个）：**

| 工具名 | 说明 |
|--------|------|
| rag_query | 基于文档的 RAG 问答 |
| list_documents | 列出用户文档 |
| generate_quiz | 生成测验题 |
| generate_mindmap | 生成思维导图/笔记 |
| generate_flashcards | 生成闪卡 |
| compare_documents | 对比文档 |

### 4.5 前端架构设计

```
Frontend/src/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 主页面（对话界面 + 侧边栏）
│   └── globals.css        # 全局样式（毛玻璃、动画、暗色模式）
├── components/             # React 组件
│   ├── chat-area.tsx      # 对话区域（流式消息、Agent 侧边栏）
│   ├── message-bubble.tsx # 消息气泡（Markdown + LaTeX + 引用上标）
│   ├── input-bar.tsx      # 输入栏（记忆模式/Agent 模式切换）
│   ├── sidebar.tsx        # 会话列表侧边栏
│   ├── settings-panel.tsx # 设置面板
│   ├── agent-sidebar.tsx  # Agent 思考链可视化侧边栏
│   ├── memory-panel.tsx   # 对话记忆可视化面板
│   └── ui/               # shadcn/ui 组件库
└── lib/                    # 工具库
    ├── api.ts             # API 请求封装（SSE 流式）
    ├── auth.ts            # 认证工具
    └── types.ts           # TypeScript 类型定义
```

**消息渲染三阶段：**

1. **思考阶段**：显示脉冲动画 + "思考中..."
2. **流式阶段**：纯文本逐字显示 + 闪烁光标（不渲染 Markdown/LaTeX，避免不完整公式丢失）
3. **完成阶段**：ReactMarkdown 完整渲染（支持 LaTeX 公式 + 引用上标 `[1]` → `<sup>[1]</sup>`）

---

## 5. 数据库设计

### 5.1 ER 图

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  users   │────→│ sessions │────→│ messages │
│          │     │          │     │          │
│ id (PK)  │     │session_id│     │ id (PK)  │
│ username │     │ user_id  │     │session_id│
│ password │     │ title    │     │ role     │
│ role     │     │ notes    │     │ content  │
│ balance  │     └──────────┘     │ sources  │
│ banned   │                      │confidence│
└──────────┘                      └──────────┘

┌──────────┐     ┌──────────┐     ┌──────────┐
│documents │     │saved_quiz│     │user_msgs │
│          │     │          │     │          │
│ id (PK)  │     │ id (PK)  │     │ id (PK)  │
│ user_id  │     │ user_id  │     │from_user │
│file_name │     │ title    │     │ to_user  │
│file_path │     │questions │     │ content  │
│ status   │     │ score    │     │ is_read  │
│full_text │     └──────────┘     └──────────┘
└──────────┘
```

### 5.2 数据表一览

| 表名 | 说明 | 记录量级 | 关键索引 |
|------|------|----------|----------|
| users | 用户表 | 百级 | username (UNIQUE) |
| sessions | 会话表 | 千级 | session_id (PK), user_id (IDX) |
| messages | 消息表 | 万级 | session_id (IDX) |
| documents | 文档表 | 百级 | user_id (IDX), status (IDX) |
| knowledge_diagnosis | 知识诊断表 | 千级 | session_id (IDX) |
| favorites | 收藏表 | 千级 | session_id (IDX) |
| reading_progress | 阅读进度表 | 百级 | user_id (IDX) |
| bookmarks | 书签表 | 百级 | user_id (IDX) |
| tags | 标签表 | 十级 | — |
| message_tags | 消息标签关联表 | 千级 | message_id, tag_id |
| saved_mindmaps | 思维导图表 | 百级 | user_id (IDX) |
| saved_quizzes | 测验表 | 百级 | user_id (IDX) |
| saved_flashcards | 闪卡表 | 百级 | user_id (IDX) |
| settings | 设置表 | 十级 | key (UNIQUE) |
| usage_logs | 使用记录表 | 万级 | user_id (IDX), created_at (IDX) |
| global_config | 全局配置表 | 行级 | num (PK) |
| user_messages | 站内信表 | 千级 | to_user_id, is_read |
| download_files | 文件下载表 | 十级 | — |

### 5.3 向量数据库设计

ChromaDB 中维护两类集合：

| 集合名 | 说明 | 嵌入维度 | 元数据 |
|--------|------|----------|--------|
| docs_{user_id} | 文档知识片段 | 512 | source, page, file_type |
| chat_{user_id} | 聊天历史记录 | 512 | session_id, question, answer, timestamp |

**嵌入模型**：BAAI/bge-small-zh-v1.5（ONNX Runtime），维度 512，CPU 推理。

### 5.4 数据库迁移策略

系统在 `DBManager.init_tables()` 中自动执行迁移：

1. 使用 `CREATE TABLE IF NOT EXISTS` 创建缺失的表
2. 使用 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 添加缺失的列
3. 自动创建 admin 默认用户（密码从环境变量读取）
4. 自动归档无主数据到 admin 用户

---

## 6. 接口设计

### 6.1 RESTful API 设计规范

- 基础路径：`/api`
- 认证方式：Bearer Token（JWT）
- 请求格式：JSON（`Content-Type: application/json`）
- 响应格式：JSON
- 流式响应：SSE（`text/event-stream`）
- 错误码：HTTP 标准状态码（200/400/401/403/404/429/500）

### 6.2 核心接口列表

#### 认证接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/auth/login` | 用户登录 | 否 |
| POST | `/api/auth/register` | 用户注册 | 否 |

#### 对话接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/chat` | 发送消息 | 是 |
| POST | `/api/chat/stream` | SSE 流式聊天 | 是 |

**请求体**：
```json
{
  "question": "用户问题",
  "session_id": "会话ID",
  "memory_mode": false
}
```

#### Agent 接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/agent/chat` | Agent 非流式对话 | 是 |
| POST | `/api/agent/stream` | Agent SSE 流式对话 | 是 |

**SSE 事件格式**：
```json
{"type": "tool_call", "tool": "rag_query", "args": {"question": "..."}, "id": "..."}
{"type": "tool_result", "tool": "rag_query", "content": "...", "id": "..."}
{"type": "token", "content": "根据文档..."}
{"type": "result", "data": {"sources": [...], "confidence": 0.85}}
{"type": "title", "data": "自动生成的标题"}
{"type": "done"}
```

#### 文档接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/upload` | 上传文档 | 是 |
| GET | `/api/documents` | 文档列表 | 是 |
| DELETE | `/api/documents/{doc_id}` | 删除文档 | 是 |

#### 会话接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/sessions` | 创建会话 | 是 |
| GET | `/api/sessions` | 会话列表（分页） | 是 |
| GET | `/api/sessions/{sid}` | 会话历史 | 是 |
| DELETE | `/api/sessions/{sid}` | 删除会话 | 是 |
| PATCH | `/api/sessions/{sid}` | 重命名会话 | 是 |

#### 管理员接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/admin/users` | 用户列表 | 管理员 |
| POST | `/api/admin/users/{uid}/balance` | 调整余额 | 管理员 |
| POST | `/api/admin/users/{uid}/ban` | 封号/解封 | 管理员 |
| GET | `/api/admin/settings/balance-ocr` | 余额 OCR 配置 | 管理员 |
| PUT | `/api/admin/settings/balance-ocr` | 更新余额 OCR 配置 | 管理员 |
| GET | `/api/admin/settings/balance-model` | 余额模型配置 | 管理员 |
| PUT | `/api/admin/settings/balance-model` | 更新余额模型配置 | 管理员 |

#### OCR 接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/ocr/cloud` | 云端 OCR 代理 | 是 |
| POST | `/api/ocr/balance` | 余额 OCR 提交任务（异步） | 是 |
| GET | `/api/ocr/balance/{job_id}` | 查询 OCR 任务状态 | 是 |

### 6.3 前端-后端通信协议

**普通模式**：前端调用 `POST /api/chat/stream`，后端通过 SSE 返回 `token` → `result` → `title` → `[DONE]` 事件。

**Agent 模式**：前端调用 `POST /api/agent/stream`，后端通过 SSE 返回 `tool_call` → `tool_result` → ... → `token` → `result` → `title` → `[DONE]` 事件。

**OCR 模式**：前端调用 `POST /api/ocr/balance` 提交任务，获得 `job_id`，然后轮询 `GET /api/ocr/balance/{job_id}` 直到状态为 `done` 或 `failed`。

---

## 7. 模块详细设计

### 7.1 RAG 查询引擎模块

**文件**：`rag/query_engine.py`

**核心方法**：`query_stream(question, session_id, llm_client, user_id, memory_mode)`

**处理流程**：

1. 输入安全校验（`sanitize_input`）
2. 获取多轮对话上下文（最近 3 轮）
3. 指代词检测与问题改写（`_has_reference` → `_rewrite_with_context`）
4. 构建历史上下文（最近 2 轮对话 + 记忆模式向量检索）
5. 执行 LangGraph 检索-评估-重写循环
6. 流式生成回答（`chat_stream`）
7. 存储助手消息和聊天记录向量
8. 自动生成会话标题（首条消息时）
9. 知识诊断记录

**关键设计决策**：
- 向量检索使用当前问题（不含历史上下文），避免历史噪声干扰嵌入
- 生成节点使用完整上下文（含历史），确保回答连贯
- 分数阈值 0.45 过滤不相关结果
- 评估阈值 0.50 直接判定相关（跳过 LLM 评估）

### 7.2 LLM 客户端模块

**文件**：`rag/llm_client.py`

**三模式架构**：

| 模式 | Provider | 数据来源 | 说明 |
|------|----------|----------|------|
| 本地 | local | settings.yaml | Ollama 本地模型 |
| 云端 | cloud | global_config (num=1) | DeepSeek 等云端 API |
| 余额 | balance | global_config (num=2) | 管理员配置的共享模型 |

**用户级 API Key**：当 provider=cloud 时，优先使用用户自己配置的 API Key（存储在 users 表）。

**DeepSeek-v4-flash 兼容**：当 `content` 为空时，检查 `reasoning_content` 字段获取回答。

### 7.3 文档管理模块

**文件**：`rag/document_manager.py`

**上传流程**：

1. 文件类型校验（仅支持 PDF）
2. 复制到永久存储目录
3. 创建 MySQL 文档记录
4. 解析 PDF（PyMuPDF，扫描型自动 OCR）
5. 文本切片（500 字符/片，50 字符重叠）
6. 向量化入库（BGE 嵌入 + ChromaDB）
7. 存储全文（供思维导图/测验使用）
8. 更新文档状态为 ready

### 7.4 Supervisor Agent 模块

**文件**：`supervisor/graph.py`

**初始化流程**：

1. 根据数据库配置构建 ChatOpenAI（支持 tool-calling）
2. 创建 6 个直接工具（`_build_direct_tools`，避免 MCP 子进程开销）
3. 构建 LangGraph ReAct Agent 图（Agent ↔ ToolNode 循环）

**工具包装**：每个工具通过 `_wrap_tools` 自动注入 `user_id` 和 `session_id`，从 schema 中移除这些字段（LLM 不可见）。

**流式输出**：使用 `stream_mode="updates"` 获取完整节点输出，yield `tool_call` → `tool_result` → `token` → `done` 事件。

### 7.5 前端消息渲染模块

**文件**：`components/message-bubble.tsx`

**三阶段渲染**：

| 阶段 | 条件 | 渲染方式 | 说明 |
|------|------|----------|------|
| 思考 | isStreaming + content="思考中..." | 脉冲动画 | 等待 LLM 响应 |
| 流式 | isStreaming + 有内容 | 纯文本 + 光标 | 逐字显示，不渲染 Markdown |
| 完成 | !isStreaming | ReactMarkdown | 完整渲染 LaTeX + 引用上标 |

**引用上标处理**：完成阶段将 `[1]` 替换为 `<sup>[1]</sup>` HTML 标签，通过 `rehypeRaw` 解析，`CiteSup` 组件拦截并添加 tooltip（来源名 + 页码）。

---

## 8. 安全设计

### 8.1 认证与授权

| 机制 | 实现 | 说明 |
|------|------|------|
| JWT Token | `api/auth.py` | 登录签发 Token，前端存 localStorage |
| 密码哈希 | bcrypt | 不可逆存储 |
| 角色控制 | role 字段 | 1=普通用户，2=管理员 |
| 权限中间件 | `get_current_user` / `get_admin_user` | 路由级权限校验 |

### 8.2 输入安全

| 机制 | 实现 | 说明 |
|------|------|------|
| 注入检测 | `security.py` | 正则匹配中英文注入模式 |
| 输入截断 | MAX_INPUT_LENGTH | 超长输入自动截断 |
| 参数化查询 | PyMySQL `%s` 占位符 | 防止 SQL 注入 |
| 文件类型校验 | 扩展名检查 | 仅允许 PDF 上传 |
| 文件大小限制 | 100MB | 上传文件大小限制 |

### 8.3 访问控制

| 机制 | 实现 | 说明 |
|------|------|------|
| 速率限制 | 内存计数器 | 每 IP 每分钟 30 次 |
| 用户隔离 | user_id 过滤 | 所有数据按用户隔离 |
| Agent 工具注入 | `_wrap_tools` | user_id 自动注入，LLM 无法越权 |
| CORS 配置 | 允许所有来源 | Bearer Token 认证，非 Cookie |

### 8.4 数据安全

| 机制 | 实现 | 说明 |
|------|------|------|
| 全局异常处理 | `@app.exception_handler` | 捕获未处理异常 |
| 日志分级 | RotatingFileHandler | DEBUG/INFO/ERROR 分级记录 |
| Agent 日志隔离 | agent.log | Agent 相关日志单独记录 |

---

## 9. 部署设计

### 9.1 Docker Compose 部署

```yaml
# docker-compose.yaml
services:
  la-mysql:
    image: mysql:8.0
    ports: ["3306:3306"]
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}

  la-api:
    build: ./Backend
    ports: ["8000:8000"]
    depends_on: [la-mysql]
    environment:
      DB_HOST: la-mysql

  la-frontend:
    build: ./Frontend
    ports: ["3000:3000"]
    depends_on: [la-api]
```

### 9.2 本地开发环境

```bash
# 后端
cd Backend
pip install -r requirements.txt
python main.py init-db    # 初始化数据库
python main.py api        # 启动后端 (localhost:8000)

# 前端
cd Frontend
npm install
npm run dev               # 启动前端 (localhost:3000)

# OCR 服务器（可选）
cd OCRServer
pip install -r requirements.txt
python main.py            # 启动 OCR 服务 (localhost:8001)
```

### 9.3 环境变量配置

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `DB_HOST` | MySQL 主机 | localhost |
| `DB_PORT` | MySQL 端口 | 3306 |
| `DB_USER` | MySQL 用户 | root |
| `DB_PASSWORD` | MySQL 密码 | 1234 |
| `DB_NAME` | 数据库名 | project |
| `JWT_SECRET` | JWT 密钥 | — |
| `OLLAMA_BASE_URL` | Ollama 地址 | http://localhost:11434 |
| `OLLAMA_MODEL` | Ollama 模型 | qwen2.5:7b-instruct |
| `OCR_SERVER_URL` | OCR 服务地址 | http://localhost:8001 |

### 9.4 日志配置

| 日志文件 | 内容 | 级别 | 轮转 |
|----------|------|------|------|
| app.log | 全部应用日志 | DEBUG | 5MB × 3 |
| error.log | 错误日志 | ERROR | 5MB × 3 |
| agent.log | Agent 相关日志 | DEBUG | 5MB × 3 |

---

## 10. 测试设计

### 10.1 测试策略

| 测试类型 | 覆盖范围 | 工具 |
|----------|----------|------|
| 单元测试 | 各模块独立功能 | pytest |
| 集成测试 | API 端点 + 数据库 | pytest + TestClient |
| 结构测试 | 模块/类/方法存在性 | pytest |
| 边界测试 | 空输入、超长输入、注入检测 | pytest |

### 10.2 测试用例分布

| 测试类 | 测试数 | 覆盖模块 |
|--------|--------|----------|
| TestConfig | 3 | 配置加载 |
| TestHelpers | 4 | 工具函数 |
| TestSecurity | 6 | 安全校验 |
| TestChunker | 5 | 文本切片 |
| TestVectorStore | 5 | 向量存储 |
| TestLLMClient | 5 | LLM 客户端 |
| TestQueryEngine | 3 | 查询引擎 |
| TestGrade | 5 | 评估节点 |
| TestDBManager | 7 | 数据库层 |
| TestModuleRefactoring | 7 | 模块结构 |
| TestBoundary | 8 | 边界条件 |
| TestAPI | 6 | API 路由 |
| 其他 | 14 | 日志、诊断、状态等 |
| **合计** | **75** | — |

### 10.3 测试运行

```bash
cd Backend
python -m pytest tests/ -v
```

---

## 附录 A：版本历史

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| V1 | 2024-06 | 初始版本，基础 RAG 问答 |
| V2 | 2024-06 | 新增前端、用户认证、思维导图、测验、闪卡、对比、统计等功能 |
| V3 | 2024-06 | 用户隔离、注册开关、封号、站内消息系统 |
| V3.1 | 2024-06 | 前后端分离（Next.js 替换 Streamlit）、PaddleOCR 开关、性能优化 |
| V4 | 2024-06 | MCP Server 封装 + Supervisor Agent + /api/agent 端点 |
| V5 | 2024-06 | OCR 服务器独立 + 记忆模式 + 设置面板 OCR 配置 |
| V6 | 2024-06 | MCP 传输改为 HTTP/SSE + Agent 思考链可视化 + 比赛版前端 |
| V7 | 2026-06 | RAG 检索优化 + OCR 异步改造 + 余额配置 + LaTeX 渲染 + 代码审查修复 |

## 附录 B：依赖清单

### 后端依赖 (requirements.txt)

```
fastapi, uvicorn, pydantic
langchain, langgraph, langchain-openai
mcp, langchain-mcp-adapters
ollama, openai
chromadb, onnxruntime, tokenizers
pymupdf, unstructured
pymysql, dbutils, cryptography
pyyaml, python-dotenv, bcrypt, pyjwt
httpx
```

### 前端依赖 (package.json)

```json
{
  "next": "15.x",
  "react": "19.x",
  "tailwindcss": "4.x",
  "react-markdown": "9.x",
  "remark-math": "6.x",
  "rehype-katex": "7.x"
}
```
