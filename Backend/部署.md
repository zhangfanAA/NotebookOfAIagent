服务端部署（VPS）

  1. 上传项目到服务器
  scp -r ./ root@你的服务器IP:/opt/learning-assistant/

  2. SSH 登录服务器执行部署
  ssh root@你的服务器IP
  cd /opt/learning-assistant
  sudo bash deploy.sh

  脚本会自动：
  - 安装 Python3 + MySQL
  - 创建数据库和用户
  - 创建 Python 虚拟环境并安装依赖（跳过 PaddleOCR，因为 OCR 在客户端本地跑）
  - 生成 .env 配置（数据库密码、JWT 密钥自动生成）
  - 创建 systemd 服务，开机自启

  3. 配置 EXEFront 客户端

  编辑 EXEFront/.env：
  VITE_API_URL=http://你的服务器IP:8000

  然后 npm run dev 启动客户端即可。

  架构

  用户电脑                          VPS 服务器
  ┌─────────────────┐              ┌──────────────────┐
  │ EXEFront 桌面端  │─── HTTP ───→│ FastAPI (8000)    │
  │ + 本地 PaddleOCR │              │ + MySQL           │
  │ + PyMuPDF       │              │ + 向量库 ChromaDB  │
  └─────────────────┘              └──────────────────┘

  - OCR/PDF 解析全在客户端本地跑，服务器压力小
  - 服务器只做 LLM 调用、向量检索、数据存储
